# Flow Music project-local session

Run `launch-flowmusic.ps1` to open Flow Music with the project-local Chrome profile.

After logging in manually, Chrome stores the session under `chrome-profile`. Keep this
folder private. Chrome encrypts cookies using the Windows user/device, so copying this
folder to another device will not make the login portable; sign in again on the other
device.
